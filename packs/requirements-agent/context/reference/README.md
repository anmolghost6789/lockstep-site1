# Reference

Place reusable reference material here, such as prior implementations, legacy BRDs, approved examples, industry references, or reusable frameworks.

Reference material can influence structure, terminology, and comparison. It does not override active inputs by default. If a reference should be treated as a source for the current run, say so in `inputs/instructions/`.
