# Project Context

Place project-specific persistent context here, including project scope, target platform defaults, implementation constraints, naming overrides, environment assumptions, and delivery standards that should be reused across runs.
