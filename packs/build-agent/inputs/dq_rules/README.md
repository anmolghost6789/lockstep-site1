# DQ Rules

Place data quality rule definitions here.

Supported formats: XLSX (recommended), CSV, DOCX, MD

The golden template `templates/DQ_RULES_TEMPLATE.xlsx` includes columns for DQ Check, Expression, Criticality, and Threshold.

Tables without DQ rules will get `[INSUFFICIENT INPUT]` stubs in the generated output.
